"""Bundled resource files (suite2p ops, AAV metadata).

Files in this directory are accessed via ``Path(__file__).parent`` from
the GUI tabs (see ``calliope.tabs.suite2p.tab``). They ship with the
package so the GUI works without depending on any external file layout.
"""
