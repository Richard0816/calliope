"""Tab 3 - Suite2p detection + cell-filter inference. See
:mod:`calliope.tabs.suite2p.tab` for the GUI class and
:mod:`calliope.tabs.suite2p.logic` for the re-export shim pointing at
:mod:`calliope.core.sparse_plus_cellpose` and
:mod:`calliope.core.cellfilter.predict`.

This file exists only so Python recognises the folder as a sub-package.
"""
