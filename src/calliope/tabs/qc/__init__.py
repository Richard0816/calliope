"""Tab 2 - QC Preview. See :mod:`calliope.tabs.qc.tab` for the GUI
class. This sub-package is small (just the QC GIF viewer) so there is
no separate ``logic`` shim; the tab calls ``preprocessing`` helpers
directly when it needs them.

This file exists only so Python recognises the folder as a sub-package.
"""
