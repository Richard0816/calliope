"""Tab 4 - Low-pass filter + Savitzky-Golay derivative. See
:mod:`calliope.tabs.lowpass.tab` for the GUI class and
:mod:`calliope.tabs.lowpass.logic` for the re-export shim pointing at
the lowpass / derivative routines in :mod:`calliope.core.utils`.

This file exists only so Python recognises the folder as a sub-package.
"""
