"""Tab 8 - Spatial propagation (per-event activation-order maps).

See :mod:`calliope.tabs.spatial_propagation.tab` for the GUI class
and :mod:`calliope.tabs.spatial_propagation.logic` for the re-export
shim. The painting helpers used by this tab live in
:mod:`calliope.core.spatial`.

This file exists only so Python recognises the folder as a sub-package.
"""

