"""Tab 1 - Input & Preprocess. See :mod:`calliope.tabs.preprocess.tab`
for the GUI class and :mod:`calliope.tabs.preprocess.logic` for the
re-export shim that points at :mod:`calliope.core.preprocessing`.

This file exists only so Python recognises the folder as a sub-package;
no code lives here on purpose.
"""
