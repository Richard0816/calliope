"""Tab 5 - Event detection (per-ROI MAD-z hysteresis + density-based
population events). See :mod:`calliope.tabs.event_detection.tab` for
the GUI class and :mod:`calliope.tabs.event_detection.logic` for the
re-export shim pointing at :mod:`calliope.core.utils` and
:mod:`calliope.core.summary_writer`.

This file exists only so Python recognises the folder as a sub-package.
"""
