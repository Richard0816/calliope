"""Tab 6 - Hierarchical clustering of ROI traces (1 - Pearson r
distance, average linkage). See :mod:`calliope.tabs.clustering.tab`
for the GUI class and :mod:`calliope.tabs.clustering.logic` for the
re-export shim pointing at :mod:`calliope.core.clustering`.

This file exists only so Python recognises the folder as a sub-package.
"""
