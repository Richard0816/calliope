# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
build/
dist/

# Virtualenvs
venv/
.venv/
env/
.env/

# Tooling
.pytest_cache/
.mypy_cache/
.ruff_cache/
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db

# Cell-filter curation data (per-curator labels, not source code)
src/calliope/data/cellfilter_labels.csv
# Cell-filter checkpoints live under ~/.calliope/cellfilter_checkpoints/
# by default (outside the repo), so no need to ignore them here -- but
# if someone overrides CHECKPOINT_DIR into the repo this catches them.
cellfilter_checkpoints/
